services:
  - type: web
    name: rabbitmq
    env: docker
    plan: free
    autoDeploy: true
    dockerfilePath: ./Dockerfile
    ports:
      - 5672 # RabbitMQ clients
      - 10000 # Management UI
